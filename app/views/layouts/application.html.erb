<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Go Tournaments" %></title>
    
    <%= favicon_link_tag 'favicon.ico' %>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">
  </head>

  <body>
    <header id="header" class="topbar-header">
    </header>
    <%= render '/layouts/topnav' %>
    <%= render '/layouts/display_notice' if notice.present? %>
    <%= render '/layouts/display_alert' if alert.present? %>

    <div class="main-content">
      <div class="grid-container-fluid">
        <%= yield %>
      </div>
    </div>
    
    <div class="modal-footer">
        <p>Designed by Hajin Lee</p>
    </div>
  </body>
</html>
